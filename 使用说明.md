# 开学能量站 · 使用说明

一套开学季的小朋友游戏合集（首页 + 6 个小游戏），可自由改成**任意老师的名字**。
改自"倩楠老师的开学能量站"，已把老师名字做成可配置。

## 里面有什么

| 页面 | 内容 |
|---|---|
| `index.html` | 首页 · 入口导航 |
| `riddle.html` | 脑筋急转弯（每天 10 题按日期更新） |
| `shout.html` | 喊出小魔星（声控大声喊） |
| `words.html` | 单词小乐园（猜字背单词） |
| `math.html` | 口算小达人 |
| `schulte.html` | 舒尔特方格（专注力） |
| `focus.html` | 番茄专注种树 |

## 怎么改成自己的名字（三选一）

**方法一（推荐，改一次全站生效）**：
用记事本打开 `config.js`，把这一行的 `"XX老师"` 改成你的名字，保存即可：
```js
window.TEACHER_NAME = "XX老师";   // ← 改成 "王老师" / "李老师" ……
```
首页和 6 个游戏会全部自动变成你的名字，不用一页页改。

**方法二（不改文件，发链接）**：
在链接后面加 `?t=名字`，例如：
```
index.html?t=王老师
```
适合把同一套游戏发给不同老师用——各自用各自的链接即可，名字还会自动记住（换页不丢）。

**方法三**：什么都不改时，显示默认的"XX老师"。

## 怎么打开来玩

- **想玩全部游戏（含"小魔星"）→ 双击 `启动游戏.cmd`**（推荐）。
  它会自动打开浏览器，玩好后关掉那个黑色窗口即可。
  > 为什么要这样：小魔星是"大声喊"的声控游戏，要用**麦克风**。浏览器有安全规定，
  > 只有通过本地服务器（`启动游戏.cmd` 就是干这个的）或网上的 `https` 网址打开，才允许用麦克风。
  > 直接双击 `index.html` 时麦克风被浏览器禁掉，所以小魔星点"开麦开始"没反应——**别直接双击 index.html**。
  > （需要电脑装了 Python；你这台已经装了。发给别的老师如果没装，就用下面"发到网上"的方式。）

- **只玩其它 5 个游戏**：双击 `index.html` 也行（脑筋急转弯 / 单词 / 口算 / 舒尔特 / 种树都不用麦克风）。

- **发给学生 / 家长（推荐）**：把整个文件夹传到任意静态托管（GitHub Pages、公司内网等），
  用 `https` 网址打开——这样**所有游戏包括小魔星都能用**，学生点链接即玩，无需装任何东西。
  - 上传时 **7 个 html + config.js + teacher.js + 启动游戏.cmd 放在同一个目录**，缺一不可。

## 原理（给想了解的人）

- 所有原来写死"倩楠老师"的地方，都换成了占位符 `{{TEACHER}}`。
- `config.js` 存名字，`teacher.js` 负责在页面打开时把占位符换成真名字；
  游戏里玩着才蹦出来的文字（比如答对提示）也会被自动兜住替换。
- 所以你永远只需要动 `config.js` 一处，或用 `?t=` 链接参数。
